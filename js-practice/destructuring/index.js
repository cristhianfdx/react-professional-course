const [a, b] = ['Hello', 'Max'];
console.log(a); // Hello
console.log(b); // Max

const { name, lastname } = { name: 'Max', lastname: 'Roberts' };
console.log(name); // Max
console.log(lastname); // Roberts
